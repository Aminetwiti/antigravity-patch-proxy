# npm config ??? applies to root + every workspace
engine-strict=true
fund=false
audit=false
save-exact=false
package-lock=true

# Color output (better log readability)
color=true

# Disable lifecycle scripts from third-party postinstalls unless explicitly trusted
ignore-scripts=false